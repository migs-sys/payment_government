# Contract

The contract stores public-sector records with owner, target amount, released amount, status, and score.

Main method:

`payPublicSectorRecord(id, actor, amount, score)`

Use this method after backend validation and wallet approval.
